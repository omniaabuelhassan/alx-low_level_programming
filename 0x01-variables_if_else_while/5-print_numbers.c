#include <stdio.h>
/**
 * main - Entry point
 * Description: program that print digit from 0 to 9
 * Return: 0(success)
 */
int main(void)
{
for (int digit = 0; digit < 10 ; digit++)
{
printf("%i", digit);
}
printf('\n');
return (0);
}
